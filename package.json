{
  "name": "inbox-secretary",
  "version": "0.1.1",
  "description": "Inbox Secretary - Obsidian Plugin",
  "main": "main.js",
  "scripts": {
    "dev": "node --env-file=.env esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "builtin-modules": "^4.0.0",
    "esbuild": "^0.25.0",
    "obsidian": "latest",
    "tslib": "^2.8.1",
    "typescript": "^5.7.0"
  }
}
